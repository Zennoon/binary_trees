#include "binary_trees.h"

/**
 * heap_insert - Inserts a node into an AVL tree
 * @tree: Douple pointer to the tree to insert to
 * @value: Value stored in the inserted node
 *
 * Return: Pointer to the created node
 */
heap_t *heap_insert(heap_t **tree, int value)
{
	tree = tree;
	value = value;
	return (NULL);
}
