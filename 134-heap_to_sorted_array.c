#include "binary_trees.h"

/**
 * heap_to_sorted_array - Makes a sorted array from a max heap
 * @heap: The heap to make the array from
 * @size: Address to store size of the array
 *
 * Return: the array
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	heap = heap;
	size = size;
	return (NULL);
}
